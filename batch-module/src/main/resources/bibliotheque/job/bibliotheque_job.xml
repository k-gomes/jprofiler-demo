<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:batch="http://www.springframework.org/schema/batch"
       xmlns:context="http://www.springframework.org/schema/context" xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/batch
        http://www.springframework.org/schema/batch/spring-batch.xsd
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context-4.0.xsd http://www.springframework.org/schema/aop https://www.springframework.org/schema/aop/spring-aop.xsd">


  <context:property-placeholder location="classpath:/properties/application.properties"/>
  <import resource="classpath:bibliotheque/configuration/datasource.xml" />
  <import resource="classpath:bibliotheque/configuration/jobRepository.xml" />


  <batch:job id="bibliothequeJob">

    <!-- ETAPE 1: -->
    <batch:step id="bibliotheque_step">
      <batch:partition step="bibliotheque_slave_step" partitioner="bibliothequePartitionner">
        <batch:handler grid-size="1" task-executor="bibliothequeTaskExecutor" />
      </batch:partition>
    </batch:step>
    <batch:listeners>
      <batch:listener ref="bibliothequeJobListener" />
    </batch:listeners>

  </batch:job>

  <batch:step id="bibliotheque_slave_step" xmlns="http://www.springframework.org/schema/batch">
    <batch:tasklet transaction-manager="transactionManager">
      <batch:chunk commit-interval="1000"
                   reader="bibliothequeReader" processor="bibliothequeProcessor" writer="bibliothequeWriter" />
    </batch:tasklet>
    <batch:listeners>
      <batch:listener ref="bibliothequeStepListener" />
    </batch:listeners>
  </batch:step>

  <bean id="bibliothequePartitionner"
        class="batch.bibliotheque.partitionner.BibliothequePartitionner" />

  <bean id="bibliothequeTaskExecutor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
    <property name="corePoolSize" value="8" />
  </bean>

  <bean id="bibliothequeStepListener"
        class="batch.bibliotheque.listener.BibliothequeStepListener"/>

  <bean id="bibliothequeJobListener"
        class="batch.bibliotheque.listener.BibliothequeJobListener"/>

  <bean id="bibliothequeReader" scope="step"
        class="batch.bibliotheque.reader.BibliothequeReader">
    <property name="executionContext" value="#{stepExecution}"/>
    <property name="jdbcTemplate" ref="jdbcTemplate" />
    <property name="pstc">
      <bean class="batch.bibliotheque.reader.BibliothequePreparedStatementCallBack">
        <property name="executionContext" value="#{stepExecution}"/>
      </bean>
    </property>
  </bean>

  <bean id="bibliothequeProcessor" scope="step"
        class="batch.bibliotheque.processor.BibliothequeProcessor">
    <property name="jdbcTemplate" ref="jdbcTemplate" />
  </bean>

  <bean id="bibliothequeWriter" scope="step"
        class="batch.bibliotheque.writer.BibliothequeWriter">
    <property name="executionContext" value="#{stepExecution}"/>
    <property name="jdbcTemplate" ref="jdbcTemplate" />
  </bean>
</beans>